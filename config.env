# TypeSpec Migration Workspace Configuration
# ============================================
# Edit these values for each service migration.
# All scripts and agent instructions reference these variables.

# --- Service Identity ---
# Human-readable service name
SERVICE_NAME="Azure Data Tables"
# TypeSpec namespace for the service (e.g. Azure.Data.Tables, Azure.KeyVault)
TYPESPEC_NAMESPACE="Azure.Data.Tables"

# --- Spec Repo Paths ---
# Path to the service spec within azure-rest-api-specs (relative to repo root)
SPEC_PATH="specification/cosmos-db/data-plane/Tables"
# Commit hash (or branch/tag) in azure-rest-api-specs containing the TypeSpec to use.
# The user provides this — it points to an already-existing TypeSpec definition.
SPEC_COMMIT="623ace17cc16f11c09e2b5264e5e2d8c066c0ae9"

# --- SDK Configuration ---
# SDK package name (npm)
SDK_PACKAGE_NAME="@azure/data-tables"
# SDK package directory within azure-sdk-for-js (relative to repo root)
SDK_PACKAGE_DIR="sdk/tables/data-tables"
# Folder containing generated code (relative to SDK package dir)
GENERATED_DIR="src/generated"

# --- Emitter Configuration ---
# The typespec-ts emitter package within autorest.typescript (relative to repo root)
EMITTER_PACKAGE_DIR="packages/typespec-ts"
# The emitter package name
EMITTER_PACKAGE_NAME="@azure-tools/typespec-ts"

# --- Repository URLs ---
SPECS_REPO="Azure/azure-rest-api-specs"
SDK_REPO="Azure/azure-sdk-for-js"
EMITTER_REPO="Azure/autorest.typescript"

# --- Workspace Paths (resolved at runtime) ---
# These are set relative to this workspace directory
WORKSPACE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
SPECS_REPO_DIR="${WORKSPACE_DIR}/azure-rest-api-specs"
SDK_REPO_DIR="${WORKSPACE_DIR}/azure-sdk-for-js"
EMITTER_REPO_DIR="${WORKSPACE_DIR}/autorest.typescript"

# Full paths derived from above
SPEC_FULL_PATH="${SPECS_REPO_DIR}/${SPEC_PATH}"
SDK_PACKAGE_FULL_PATH="${SDK_REPO_DIR}/${SDK_PACKAGE_DIR}"
GENERATED_FULL_PATH="${SDK_PACKAGE_FULL_PATH}/${GENERATED_DIR}"
EMITTER_FULL_PATH="${EMITTER_REPO_DIR}/${EMITTER_PACKAGE_DIR}"

# --- Backup location for old generated code ---
BACKUP_DIR="${WORKSPACE_DIR}/.generated-backup"
